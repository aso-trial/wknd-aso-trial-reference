#
# This file contains the cache rules, and can be customized.
#
# By default, it includes the default rules.
#

$include "./default_rules.any"

# Allow caching for Core Image Component renditions (LCP optimization)
/0100 {
    /glob "*.coreimg.*"
    /type "allow"
}
